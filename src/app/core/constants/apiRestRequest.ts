//Application enums

export enum ApiMethod {
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE",
}

export enum AuthEndPoints {
    register = "/users"
}

export enum Webrtc {
    broadcast = '/broadcast',
    consumer = '/consumer'
}